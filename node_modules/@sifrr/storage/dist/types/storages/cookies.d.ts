import Storage from './storage';
import { StorageOptions } from './types';
declare class Cookies extends Storage {
    constructor(options: StorageOptions);
    protected upsert(data: object): boolean;
    protected delete(keys: string[]): boolean;
    protected deleteAll(): Promise<boolean>;
    protected getStore(): {};
    protected setStore(v: string): void;
    protected hasStore(): boolean;
    static get type(): string;
}
export default Cookies;
