import Storage from './storage';
import { StorageOptions } from './types';
declare class IndexedDB extends Storage {
    private store;
    constructor(options: StorageOptions);
    protected select(keys: string[]): Promise<{}>;
    protected upsert(data: object): Promise<boolean>;
    protected delete(keys: string[]): Promise<boolean>;
    protected deleteAll(): any;
    private _tx;
    protected getStore(): any;
    private createStore;
    protected hasStore(): boolean;
    static get type(): string;
}
export default IndexedDB;
